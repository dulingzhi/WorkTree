# Locale files are keyed by filename (`settings.en.yml` -> locale "en"), so the
# content below is the key tree directly — no `en:` root. English is also the
# fallback locale, so every value must stay byte-identical to the string it
# replaces in `src/view/settings_window.rs` (tests assert these exact texts).
settings:
  window:
    title: "Settings: GitComet"
  nav:
    general: General
    terminal: Terminal
    change_tracking: Change tracking
    diff: Diff
    file_editing: File editing
    git_log: Git log
    tags: Tags
    git_executable: Git executable
    environment: Environment
    links: Links
    no_match: No matching settings
  search:
    placeholder: Search
  section:
    appearance: Appearance
    integrations: Integrations
    date_time: "Date & Time"
  row:
    theme: Theme
    date_format: Date format
    ui_scale: UI scale
    ui_font: UI Font
    editor_font: Editor Font
    font_ligatures: Use font ligatures
    external_code_editor: External code editor
    date_timezone: Date timezone
    show_timezone: Show timezone
    external_terminal: External terminal
    action_bar_terminal: Action bar terminal button opens
    untracked_files: Untracked files
    scroll_sync: Scroll sync
    diff_mode: Diff mode
    show_whitespace_changes: Show whitespace changes
    reveal_whitespace_characters: Reveal whitespace characters
    word_wrap: Word wrap
    show_line_numbers: Show line numbers
    default_history_mode: Default history mode
    history_columns: History columns
    highlight_commit_lane: Highlight selected commit lane
    relative_dates: Relative dates in history view
    show_tags: Show tags in history view
    auto_fetch_tags: Automatically fetch tags
    view_mode: View mode
    auto_save: Auto-save edits
    open_theme_folder: Open custom theme folder
    theme_guide: Theme guide
    avatar_source: Commit author avatars
    ai_commit: AI commit messages
  avatar_source:
    option_initials: "Initials (current, offline)"
    option_gravatar: "Gravatar (gravatar.com)"
    option_cravatar: "Cravatar (cravatar.cn)"
  ai_commit:
    provider_anthropic: "Anthropic"
    provider_openai: "OpenAI compatible"
    summary_unconfigured: "%{provider} (no API key)"
    model: Model
    api_key: API key
    api_key_placeholder: "sk-…"
    endpoint: "API endpoint (optional)"
    models_fetch: "Fetch list"
    models_loading: "Fetching model list…"
    models_failed: "Failed to fetch models: %{error}"
    privacy_hint: "The API key is stored locally in the session file. Generating a message sends the staged diff to the selected service."
  ui_scale:
    detail_default: Default scale
    detail_fit_more: Fit more on screen
    detail_larger: Larger controls and text
    shortcut: "Shortcut: Ctrl/Cmd +, -, and 0."
  fonts:
    empty: No fonts available.
    system_detail: "Use GitComet's best match for the operating system UI font stack"
    choose_hint: Choose from installed system fonts
  external_editor:
    prompt_select: Select external code editor
    custom_executable: Custom editor executable
    arguments: Arguments
  terminal:
    prompt_select_launcher: Select terminal launcher
    note_best_effort: "System default is best effort. Use a custom launcher for predictable cross-platform behavior."
    default_detail: Use the platform default when possible
    custom_detail: Choose a launcher and explicit arguments
    program: Program
    arguments: Arguments
    args_placeholder: One argument per line
    args_hint: "One argument per line. Use {cwd} and {repo_name} placeholders."
    status_saved: External terminal settings saved.
    status_reset: External terminal draft reset.
    status_launch_sent: Launch request sent.
    status_launch_failed: "Test launch failed: %{err}"
  action_bar_terminal:
    note: "Choose what the action bar terminal button opens. Global shortcuts for each can be configured separately."
    embedded_detail: Toggle the embedded terminal panel
    external_detail: Launch the external terminal
  action:
    browse: Browse
    save: Save
    reset: Reset
    test_launch: Test launch
    show: Show
  git_log:
    default_mode_note: Applies when opening repositories that do not already have a saved history mode.
    column_graph: Graph
    column_author: Author
    column_date: Commit date
    column_sha: SHA
    columns_none: None
    columns_note: Columns may auto-hide in narrow windows.
    reset_column_widths: Reset column widths
  tags:
    fetch_on_activation: On repository activation
    fetch_on_activation_detail: Fetch local and remote tags in the background when a repository becomes active.
    fetch_disabled: Disabled
    fetch_disabled_detail: Skip automatic tag fetching on repository activation.
    lightweight: Lightweight
    lightweight_detail: A simple tag pointing directly to a commit. No message, no GPG signing.
    annotated: Annotated
    annotated_detail: Stores tag author, date, and an optional message. Supports GPG signing.
  git_executable:
    system_path: System PATH
    system_detail: "Use the first `git` executable available in the current PATH."
    custom: Custom executable
    custom_detail: Use a specific Git binary and add its directory when Git resolves helper tools.
    prompt_select: Select Git executable
    use_path: Use Path
    custom_label: Custom Git executable
    enter_hint: Press Enter after editing the path to apply it immediately.
    scope_note: Applies to the main GitComet browser window. Git-invoked command modes keep using git from System PATH. Helper tools such as gpg are resolved by Git from the app environment unless configured in Git.
  git:
    detected_runtime: Detected runtime
    status_supported: "Git >= %{version}"
    status_too_old: "Git < %{version}"
    status_unknown: Git version unknown
    compatibility_note: GitComet has been tested only with Git %{version} or newer.
  common:
    unavailable: Unavailable
  environment:
    build: Build
    operating_system: Operating system
  links:
    license: License
    professional_waitlist: Professional Edition waitlist
    open_source_licenses: Open source licenses
  licenses:
    back: "< Settings"
    empty: No dependency licenses found.
    count: "%{count} third-party crates listed"
    column_crate: Crate
    column_version: Version
    column_license: License
  theme:
    folder_toast_unavailable: Custom theme folder is unavailable.
    folder_open_failed: "Failed to open custom theme folder: %{err}"
  change_tracking:
    combined_detail: Keep untracked files inside the Unstaged section
    split_untracked_detail: Show an Untracked block above Unstaged
  diff:
    scroll_sync_vertical_detail: Lock vertical scrolling only.
    scroll_sync_horizontal_detail: Lock horizontal scrolling only.
    scroll_sync_none_detail: Keep split and merge panes independent.
    scroll_sync_both_detail: Lock both vertical and horizontal scrolling.
    content_collapsed_detail: Hide unchanged sections, with hunk controls to reveal more context.
    content_full_detail: Show the full file using the regular file diff view.
    view_inline_detail: Show changes inline.
    view_split_detail: Show changes in split view.
