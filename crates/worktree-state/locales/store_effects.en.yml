store:
  effects:
    clone_url_empty: "clone URL cannot be empty"
    clone_url_unsupported_scheme: "unsupported clone URL scheme `%{scheme}` (allowed: https, ssh, git, file)"
    clone_url_invalid_format: "invalid clone URL format for `%{scheme}`; expected `%{scheme}://...`"
    ssh_host_verification_prompt: "SSH host verification prompt:\n"
    clone_aborted_cleanup_inspect_failed: "clone aborted, but failed to inspect partially created destination `%{dest}`: %{err}"
    clone_aborted_cleanup_remove_failed: "clone aborted, but failed to remove partially created destination `%{dest}`: %{err}"
    git_command_timed_out: "%{command} timed out after %{secs} seconds (set %{env} to override)"
    outside_workdir_error: "refusing to write outside repository workdir"
    worktree_path_empty: "worktree file path must not be empty"
    save_worktree_file_label: "Save %{path}"
    save_worktree_file_staged_label: "Save %{path} (staged)"
    gitignore_not_utf8: "%{file} is not valid UTF-8; edit it manually"
    update_gitignore_label: "Update %{file}"
    mergetool_label: "mergetool (%{tool})"
    mergetool_failed: "Mergetool '%{tool}' did not complete successfully"
    repo_handle_not_found: "Repository handle not found for repo_id %{repo_id}"
    branch_file_listing_unimplemented: "branch file listing is not yet implemented"
    no_commit_message: "no message for commit %{commit}"
    open_parent_commit_failed: "Could not open file at parent commit: %{err}"
    repo_unavailable_cherry_pick_messages: "repository unavailable while loading cherry-pick commit messages"
    branch_noun_one: "branch"
    branch_noun_other: "branches"
    deleted_n_of_m: "Deleted %{deleted} of %{total} %{noun}. Failed: %{names}"
    failure_detail_suffix: ". %{error}"
    need_force_delete_suffix: ". Branches that are not fully merged need Force delete."
