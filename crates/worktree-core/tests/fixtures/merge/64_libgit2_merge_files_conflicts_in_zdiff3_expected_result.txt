#@ opts.style=zdiff3
#@ opts.label.ours=file.txt
#@ opts.label.base=file.txt
#@ opts.label.theirs=file.txt
#@ expect.clean=true
#@ expect.conflict_count=0
# Contributor alignment anchors the shared replacement and keeps the remote-only insertion.
