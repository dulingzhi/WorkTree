name: Deployment CI

on:
  pull_request:
    branches: ["main", "dev"]
    paths:
      - ".github/workflows/build-release-artifacts.yml"
      - ".github/workflows/deploy-aur.yml"
      - ".github/workflows/deploy-homebrew-tap.yml"
      - ".github/workflows/deploy-apt-repo.yml"
      - ".github/workflows/deploy-windows-installer.yml"
      - ".github/workflows/deploy-microsoft-store.yml"
      - ".github/workflows/release-manual-main.yml"
      - "scripts/update-aur.sh"
      - "scripts/cargo-flatten-dupes.sh"
      - "scripts/package-macos.sh"
      - "scripts/macos-cargo-config.sh"
      - "scripts/generate-homebrew-cask.sh"
      - "scripts/build-apt-repo.sh"
      - "scripts/windows/verify-signed-artifact.ps1"
      - "scripts/microsoft-store-fetch-listings.sh"
      - "scripts/microsoft-store-listing-languages.sh"
      - "scripts/microsoft-store-listing-metadata.sh"
      - "scripts/microsoft-store-normalize-json.sh"
      - "scripts/microsoft-store-prepare-metadata.sh"
      - "scripts/test-microsoft-store-credentials.sh"
      - "scripts/test-microsoft-store-fetch-listings.sh"
      - "scripts/test-microsoft-store-listing-languages.sh"
      - "scripts/test-microsoft-store-workflow.sh"
  push:
    branches: ["main", "dev"]
    paths:
      - ".github/workflows/build-release-artifacts.yml"
      - ".github/workflows/deploy-aur.yml"
      - ".github/workflows/deploy-homebrew-tap.yml"
      - ".github/workflows/deploy-apt-repo.yml"
      - ".github/workflows/deploy-windows-installer.yml"
      - ".github/workflows/deploy-microsoft-store.yml"
      - ".github/workflows/release-manual-main.yml"
      - "scripts/update-aur.sh"
      - "scripts/cargo-flatten-dupes.sh"
      - "scripts/package-macos.sh"
      - "scripts/macos-cargo-config.sh"
      - "scripts/generate-homebrew-cask.sh"
      - "scripts/build-apt-repo.sh"
      - "scripts/windows/verify-signed-artifact.ps1"
      - "scripts/microsoft-store-fetch-listings.sh"
      - "scripts/microsoft-store-listing-languages.sh"
      - "scripts/microsoft-store-listing-metadata.sh"
      - "scripts/microsoft-store-normalize-json.sh"
      - "scripts/microsoft-store-prepare-metadata.sh"
      - "scripts/test-microsoft-store-credentials.sh"
      - "scripts/test-microsoft-store-fetch-listings.sh"
      - "scripts/test-microsoft-store-listing-languages.sh"
      - "scripts/test-microsoft-store-workflow.sh"
  workflow_dispatch:

permissions:
  contents: read

env:
  CARGO_TERM_COLOR: always

jobs:
  formula-script-validation:
    name: Validate release helper scripts and workflow YAML
    runs-on: ubuntu-22.04
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v7

      - name: Lint GitHub Actions workflows
        env:
          ACTIONLINT_VERSION: 1.7.12
        run: |
          set -euo pipefail

          installer="$RUNNER_TEMP/download-actionlint.bash"
          curl --fail --silent --show-error --location \
            "https://raw.githubusercontent.com/rhysd/actionlint/v${ACTIONLINT_VERSION}/scripts/download-actionlint.bash" \
            --output "$installer"
          bash "$installer" "$ACTIONLINT_VERSION" "$RUNNER_TEMP"
          # Shell scripts are checked separately below. Disabling actionlint's
          # ShellCheck integration keeps this gate focused on workflow syntax,
          # expressions, events, reusable-workflow inputs, and action metadata.
          "$RUNNER_TEMP/actionlint" -color -shellcheck=""

      - name: Validate shell scripts syntax
        run: |
          bash -n scripts/cargo-flatten-dupes.sh
          bash -n scripts/update-aur.sh
          bash -n scripts/package-macos.sh
          bash -n scripts/notarize-macos.sh
          bash -n scripts/macos-cargo-config.sh
          bash -n scripts/generate-homebrew-cask.sh
          bash -n scripts/build-apt-repo.sh
          bash -n scripts/microsoft-store-fetch-listings.sh
          bash -n scripts/microsoft-store-listing-languages.sh
          bash -n scripts/microsoft-store-listing-metadata.sh
          bash -n scripts/microsoft-store-normalize-json.sh
          bash -n scripts/microsoft-store-prepare-metadata.sh
          bash -n scripts/test-microsoft-store-credentials.sh
          bash -n scripts/test-microsoft-store-fetch-listings.sh
          bash -n scripts/test-microsoft-store-listing-languages.sh
          bash -n scripts/test-microsoft-store-workflow.sh

      - name: Validate Windows PowerShell helper syntax
        run: |
          pwsh -NoLogo -NoProfile -Command '
            $errors = $null
            [System.Management.Automation.Language.Parser]::ParseFile("scripts/windows/verify-signed-artifact.ps1", [ref]$null, [ref]$errors) | Out-Null
            if ($errors.Count -gt 0) {
              $errors | ForEach-Object { Write-Error $_.Message }
              exit 1
            }
          '

      - name: Validate workflow YAML
        run: |
          ruby -e 'require "yaml"; %w[
            .github/workflows/build-release-artifacts.yml
            .github/workflows/deploy-aur.yml
            .github/workflows/deploy-homebrew-tap.yml
            .github/workflows/deploy-apt-repo.yml
            .github/workflows/deploy-windows-installer.yml
            .github/workflows/deploy-microsoft-store.yml
            .github/workflows/deployment-ci.yml
            .github/workflows/release-manual-main.yml
          ].each { |path| YAML.load_file(path) }'

      - name: Validate deployment workflow config keys
        run: |
          set -euo pipefail
          # The release flow currently publishes to GitHub Releases only; the
          # deploy-* reusable workflows are validated below but are not wired
          # into release-manual-main.yml.
          grep -Fq 'vars.APT_STORAGE_ACCOUNT' .github/workflows/deploy-apt-repo.yml
          grep -Fq 'vars.APT_STORAGE_CONTAINER' .github/workflows/deploy-apt-repo.yml
          grep -Fq 'vars.APT_REPO_DISTRIBUTION' .github/workflows/deploy-apt-repo.yml
          grep -Fq 'vars.APT_REPO_COMPONENT' .github/workflows/deploy-apt-repo.yml
          grep -Fq 'vars.APT_REPO_ORIGIN' .github/workflows/deploy-apt-repo.yml
          grep -Fq 'vars.APT_REPO_LABEL' .github/workflows/deploy-apt-repo.yml
          grep -Fq 'vars.APT_REPO_DESCRIPTION' .github/workflows/deploy-apt-repo.yml
          grep -Fq 'vars.APT_STORAGE_PUBLIC_ACCESS' .github/workflows/deploy-apt-repo.yml
          grep -Fq 'vars.APT_STORAGE_ACCOUNT' .github/workflows/deploy-windows-installer.yml
          grep -Fq 'AZURE_STORAGE_ACCOUNT_KEY' .github/workflows/deploy-windows-installer.yml
          grep -Fq 'windows-x86_64.msi' .github/workflows/deploy-windows-installer.yml
          grep -Fq 'windows-arm64.msi' .github/workflows/deploy-windows-installer.yml
          grep -Fq 'vars.MICROSOFT_STORE_PACKAGE_BASE_URL' .github/workflows/deploy-microsoft-store.yml
          grep -Fq 'vars.MICROSOFT_STORE_PRODUCT_ID' .github/workflows/deploy-microsoft-store.yml
          grep -Fq 'vars.MICROSOFT_STORE_PACKAGE_LANGUAGES' .github/workflows/deploy-microsoft-store.yml
          grep -Fq 'MICROSOFT_STORE_TENANT_ID' .github/workflows/deploy-microsoft-store.yml
          grep -Fq 'MICROSOFT_STORE_CLIENT_ID' .github/workflows/deploy-microsoft-store.yml
          grep -Fq 'MICROSOFT_STORE_CLIENT_SECRET' .github/workflows/deploy-microsoft-store.yml
          grep -Fq 'MICROSOFT_STORE_SELLER_ID' .github/workflows/deploy-microsoft-store.yml
          grep -Fq 'dbus-launch --sh-syntax' .github/workflows/deploy-microsoft-store.yml
          grep -Fq 'gnome-keyring-daemon --start --components=secrets' .github/workflows/deploy-microsoft-store.yml
          grep -Fq 'libsecret-1-0' .github/workflows/deploy-microsoft-store.yml
          grep -Fq 'runs-on: ubuntu-24.04' .github/workflows/deploy-microsoft-store.yml
          grep -Fq 'actions/setup-dotnet@c2fa09f4bde5ebb9d1777cf28262a3eb3db3ced7 # v5.2.0' .github/workflows/deploy-microsoft-store.yml
          grep -Fq 'dotnet-version: "9.0.x"' .github/workflows/deploy-microsoft-store.yml
          grep -Fq "Microsoft.NETCore.App 9" .github/workflows/deploy-microsoft-store.yml
          grep -Fq 'microsoft/microsoft-store-apppublisher@cc9910a8d59f2eb55cbb83df0a3800cf3b5300e0 # v1.4' .github/workflows/deploy-microsoft-store.yml
          grep -Fq 'version: v0.3.9' .github/workflows/deploy-microsoft-store.yml
          grep -Fq 'store_preflight:' .github/workflows/deploy-microsoft-store.yml
          grep -Fq 'gh release view' .github/workflows/deploy-microsoft-store.yml
          grep -Fq 'msstore submission update' .github/workflows/deploy-microsoft-store.yml
          grep -Fq 'msstore submission updateMetadata' .github/workflows/deploy-microsoft-store.yml
          grep -Fq 'msstore submission publish' .github/workflows/deploy-microsoft-store.yml
          grep -Fq 'd0cf11e0a1b11ae1' .github/workflows/deploy-microsoft-store.yml
          grep -Fq -e '--module listings' scripts/microsoft-store-fetch-listings.sh
          grep -Fq 'microsoft-store-fetch-listings.sh' scripts/microsoft-store-prepare-metadata.sh
          grep -Fq 'scripts/microsoft-store-listing-metadata.sh' .github/workflows/deploy-microsoft-store.yml
          grep -Fq 'scripts/microsoft-store-prepare-metadata.sh' .github/workflows/deploy-microsoft-store.yml
          grep -Fq 'microsoft-store-listing-languages.sh' scripts/test-microsoft-store-credentials.sh
          ! grep -Fq 'en-us,en' .github/workflows/deploy-microsoft-store.yml
          grep -Fq 'vars.MICROSOFT_STORE_LISTING_LANGUAGES' .github/workflows/deploy-microsoft-store.yml
          grep -Fq 'WhatsNew' scripts/microsoft-store-listing-metadata.sh
          ! grep -Fq 'msstore submission commit' .github/workflows/deploy-microsoft-store.yml
          ! grep -Fq 'ReleaseNotes' .github/workflows/deploy-microsoft-store.yml
          grep -Fq '"X64"' .github/workflows/deploy-microsoft-store.yml
          grep -Fq '"Arm64"' .github/workflows/deploy-microsoft-store.yml
          grep -Fq 'vars.AUR_GIT_REPOSITORY' .github/workflows/deploy-aur.yml
          grep -Fq 'vars.AUR_GIT_BRANCH' .github/workflows/deploy-aur.yml
          grep -Fq 'vars.WINDOWS_SIGNING_ENDPOINT' .github/workflows/build-release-artifacts.yml
          grep -Fq 'vars.WINDOWS_SIGNING_ACCOUNT_NAME' .github/workflows/build-release-artifacts.yml
          grep -Fq 'AZURE_CREDENTIALS' .github/workflows/build-release-artifacts.yml
          grep -Fq 'worktree-windows-test' .github/workflows/build-release-artifacts.yml
          grep -Fq 'worktree-windows-release' .github/workflows/build-release-artifacts.yml
          grep -Fq 'azure/login@' .github/workflows/build-release-artifacts.yml
          grep -Fq 'artifact-signing-action@' .github/workflows/build-release-artifacts.yml
          grep -Fq 'file-digest:' .github/workflows/build-release-artifacts.yml
          grep -Fq 'timestamp-rfc3161:' .github/workflows/build-release-artifacts.yml
          grep -Fq 'timestamp-digest:' .github/workflows/build-release-artifacts.yml
          grep -Fq 'append-signature:' .github/workflows/build-release-artifacts.yml
          grep -Fq 'verify-signed-artifact.ps1' .github/workflows/build-release-artifacts.yml
          grep -Fq 'AUR_PRIVATE_SSH_KEY' .github/workflows/deploy-aur.yml
          grep -Fq 'AUR_PRIVATE_SSH_KEY_PASSPHRASE' .github/workflows/deploy-aur.yml
          grep -Fq 'ssh://aur@aur.archlinux.org/worktree.git' .github/workflows/deploy-aur.yml
          grep -Fq 'HOMEBREW_TAP_TOKEN' .github/workflows/deploy-homebrew-tap.yml

      - name: Test Microsoft Store deployment workflow
        env:
          ACTIONLINT: ${{ runner.temp }}/actionlint
        run: |
          scripts/test-microsoft-store-workflow.sh

      - name: Generate Microsoft Store listing metadata from synthetic draft
        run: |
          set -euo pipefail
          mkdir -p dist/deployment-ci/microsoft-store
          store_dir="dist/deployment-ci/microsoft-store"
          release_url="https://github.com/dulingzhi/WorkTree/releases/tag/v0.0.0-ci"

          cat > "$store_dir/listings.json" <<'JSON'
          {
            "Listings": [
              {
                "Language": "en-us",
                "Description": "WorkTree is a fast Git client.",
                "WhatsNew": "stale notes from the previous release",
                "ShortDescription": "Fast Git client",
                "SearchTerms": ["git", "vcs"],
                "AdditionalLicenseTerms": "MIT",
                "Requirements": null
              },
              {
                "Language": "en-gb",
                "Description": "WorkTree is a fast Git client.",
                "AdditionalLicenseTerms": "MIT"
              }
            ]
          }
          JSON

          {
            echo '## New Visuals'
            echo '<img width="1987" src="https://example.invalid/screenshot.png" />'
            echo
            echo "## What's Changed"
            echo '* Fix/repo ends with dot git by @quakj in https://github.com/dulingzhi/WorkTree/pull/276'
            for pr in $(seq 300 400); do
              echo "* Change number ${pr} with a deliberately long title by @someone in https://github.com/dulingzhi/WorkTree/pull/${pr}"
            done
            echo '**Full Changelog**: https://github.com/dulingzhi/WorkTree/compare/v0.1.0...v0.2.0'
          } > "$store_dir/release-notes.txt"

          scripts/microsoft-store-listing-metadata.sh \
            --listings-json "$store_dir/listings.json" \
            --release-notes "$store_dir/release-notes.txt" \
            --release-url "$release_url" \
            --output-dir "$store_dir/payloads" \
            > "$store_dir/payloads.txt"

          test "$(wc -l < "$store_dir/payloads.txt")" -eq 2

          expected_languages=("en-us" "en-gb")
          index=0
          while read -r payload; do
            test -f "$payload"
            # The metadata endpoint is a PUT, so every field of the draft listing has to
            # survive the round trip; only WhatsNew may change.
            test "$(jq -r 'keys | join(",")' "$payload")" = "Listings"
            test "$(jq -r '.Listings.Language' "$payload")" = "${expected_languages[$index]}"
            test "$(jq -r '.Listings.Description' "$payload")" = "WorkTree is a fast Git client."
            test "$(jq -r '.Listings.AdditionalLicenseTerms' "$payload")" = "MIT"

            whats_new="$(jq -r '.Listings.WhatsNew' "$payload")"
            test "$(jq -r '.Listings.WhatsNew | length' "$payload")" -le 1500
            printf '%s' "$whats_new" | grep -Fq "$release_url"
            printf '%s' "$whats_new" | grep -Fq '(#276)'
            ! printf '%s' "$whats_new" | grep -Fq '<img'
            ! printf '%s' "$whats_new" | grep -Fq '](http'
            ! printf '%s' "$whats_new" | grep -Fq 'Full Changelog'
            ! printf '%s' "$whats_new" | grep -Fq 'stale notes'

            index=$((index + 1))
          done < "$store_dir/payloads.txt"

          # The v0.2.0 release failed because the packages payload was fetched instead of
          # the listings module; that must be a loud failure, not an empty submission.
          echo '{"Packages":[{"PackageUrl":"https://example.invalid/worktree.msi"}]}' \
            > "$store_dir/packages.json"
          if scripts/microsoft-store-listing-metadata.sh \
            --listings-json "$store_dir/packages.json" \
            --release-notes "$store_dir/release-notes.txt" \
            --release-url "$release_url" \
            --output-dir "$store_dir/payloads-invalid" \
            > /dev/null 2>&1; then
            echo "Expected listing metadata generation to fail when the draft has no listings."
            exit 1
          fi

      - name: Generate Homebrew cask from synthetic artifacts
        run: |
          set -euo pipefail
          mkdir -p dist/deployment-ci
          printf 'linux-arm-appimage-test' > dist/deployment-ci/worktree-v0.0.0-ci-linux-arm64.AppImage
          printf 'linux-appimage-test' > dist/deployment-ci/worktree-v0.0.0-ci-linux-x86_64.AppImage
          printf 'arm64-dmg-test' > dist/deployment-ci/worktree-v0.0.0-ci-macos-arm64.dmg
          printf 'intel-dmg-test' > dist/deployment-ci/worktree-v0.0.0-ci-macos-x86_64.dmg

          scripts/generate-homebrew-cask.sh \
            --version "0.0.0-ci" \
            --github-repo "dulingzhi/WorkTree" \
            --arm-dmg "dist/deployment-ci/worktree-v0.0.0-ci-macos-arm64.dmg" \
            --intel-dmg "dist/deployment-ci/worktree-v0.0.0-ci-macos-x86_64.dmg" \
            --linux-arm-appimage "dist/deployment-ci/worktree-v0.0.0-ci-linux-arm64.AppImage" \
            --linux-intel-appimage "dist/deployment-ci/worktree-v0.0.0-ci-linux-x86_64.AppImage" \
            --output "dist/deployment-ci/worktree.rb"

          ruby -c dist/deployment-ci/worktree.rb
          grep -q 'cask "worktree" do' dist/deployment-ci/worktree.rb
          grep -q 'os macos: "macos", linux: "linux"' dist/deployment-ci/worktree.rb
          grep -q 'on_linux do' dist/deployment-ci/worktree.rb
          grep -q 'on_macos do' dist/deployment-ci/worktree.rb
          grep -q 'app "WorkTree.app"' dist/deployment-ci/worktree.rb
          grep -q 'binary "#{appdir}/WorkTree.app/Contents/MacOS/worktree", target: "worktree"' dist/deployment-ci/worktree.rb
          grep -q 'container type: :naked' dist/deployment-ci/worktree.rb
          grep -q 'binary "worktree-v#{version}-linux-#{arch}.AppImage", target: "worktree"' dist/deployment-ci/worktree.rb
          grep -q 'worktree-v#{version}-linux-#{arch}.AppImage' dist/deployment-ci/worktree.rb
          grep -q 'depends_on macos:' dist/deployment-ci/worktree.rb

      - name: Validate Homebrew tap publish detection
        run: |
          set -euo pipefail
          rm -rf dist/deployment-ci/tap-repo
          git init --initial-branch=main dist/deployment-ci/tap-repo

          pushd dist/deployment-ci/tap-repo >/dev/null
          git config user.name "WorkTree CI"
          git config user.email "ci@autoexplore.ai"
          echo "# Homebrew Tap" > README.md
          mkdir -p Formula
          cat > Formula/worktree.rb <<'RUBY'
          class Worktree < Formula
            desc "Legacy WorkTree formula"
            homepage "https://example.com"
            url "https://example.com/worktree.tar.gz"
            sha256 "aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa"
            version "0.0.0"
          end
          RUBY
          cat > Formula/worktree-cli.rb <<'RUBY'
          class WorktreeCli < Formula
            desc "Legacy WorkTree CLI formula"
            homepage "https://example.com"
            url "https://example.com/worktree-cli.tar.gz"
            sha256 "bbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbb"
            version "0.0.0"
          end
          RUBY

          git add README.md Formula/worktree.rb Formula/worktree-cli.rb
          git commit -m "Initial tap repo"

          mkdir -p Casks
          cp ../worktree.rb Casks/worktree.rb

          git add Casks/worktree.rb
          if git ls-files --error-unmatch Formula/worktree-cli.rb >/dev/null 2>&1; then
            git rm -f Formula/worktree-cli.rb
          fi
          if git ls-files --error-unmatch Formula/worktree.rb >/dev/null 2>&1; then
            git rm -f Formula/worktree.rb
          fi

          if git diff --cached --quiet -- Casks/worktree.rb Formula/worktree-cli.rb Formula/worktree.rb; then
            echo "Expected Homebrew cask publish and legacy formula removal to be detected as a change."
            exit 1
          fi
          popd >/dev/null

  aur-metadata-smoke:
    name: Validate AUR metadata update script
    runs-on: ubuntu-22.04
    timeout-minutes: 20
    container:
      image: archlinux:base-devel
    steps:
      - name: Install Arch packaging tooling
        run: |
          set -euo pipefail
          pacman -Sy --noconfirm --needed perl shadow

      - uses: actions/checkout@v7

      - name: Create non-root packaging user
        run: |
          set -euo pipefail
          id -u builder >/dev/null 2>&1 || useradd -m builder
          chown -R builder:builder "$GITHUB_WORKSPACE"

      - name: Prepare synthetic AUR repo and assets
        run: |
          set -euo pipefail
          mkdir -p dist/aur-ci/repo
          cat > dist/aur-ci/repo/PKGBUILD <<'EOF'
          pkgname=worktree
          pkgver=0.0.0
          pkgrel=1
          pkgdesc="Fast, resource-efficient Git GUI written in Rust"
          arch=('x86_64')
          url="https://worktree.dev/"
          license=('AGPL-3.0-only')
          depends=(
            'fontconfig'
            'freetype2'
            'git'
            'libx11'
            'libxcb'
            'libxkbcommon'
            'wayland'
          )
          source=(
            "worktree-v$pkgver-linux-x86_64.tar.gz::https://example.invalid/worktree-v$pkgver-linux-x86_64.tar.gz"
            "worktree-source-v$pkgver.tar.gz::https://example.invalid/worktree-source-v$pkgver.tar.gz"
          )
          sha256sums=('oldbinary'
                      'oldsource')

          package() {
            install -D -m755 "worktree-v$pkgver-linux-x86_64/worktree" "$pkgdir/usr/bin/worktree"
          }
          EOF

          printf 'synthetic-binary' > dist/aur-ci/worktree-v0.0.0-linux-x86_64.tar.gz
          printf 'synthetic-source' > dist/aur-ci/worktree-source-v0.0.0.tar.gz
          chown -R builder:builder dist

      - name: Run update-aur.sh
        run: |
          set -euo pipefail
          su builder -c "cd '$GITHUB_WORKSPACE' && scripts/update-aur.sh \
            --aur-dir '$GITHUB_WORKSPACE/dist/aur-ci/repo' \
            --version 0.0.0 \
            --binary-tar '$GITHUB_WORKSPACE/dist/aur-ci/worktree-v0.0.0-linux-x86_64.tar.gz' \
            --source-tar '$GITHUB_WORKSPACE/dist/aur-ci/worktree-source-v0.0.0.tar.gz' \
            --verify-source"

      - name: Validate generated PKGBUILD and .SRCINFO
        run: |
          set -euo pipefail
          test -f dist/aur-ci/repo/.SRCINFO
          grep -q '^pkgver=0.0.0$' dist/aur-ci/repo/PKGBUILD
          grep -q 'source = worktree-v0.0.0-linux-x86_64.tar.gz::https://example.invalid/worktree-v0.0.0-linux-x86_64.tar.gz$' dist/aur-ci/repo/.SRCINFO
          grep -q 'source = worktree-source-v0.0.0.tar.gz::https://example.invalid/worktree-source-v0.0.0.tar.gz$' dist/aur-ci/repo/.SRCINFO

  apt-repo-generation-smoke:
    name: Validate APT repo generation script
    runs-on: ubuntu-22.04
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v7

      - name: Install Debian repo tooling
        run: |
          sudo apt-get update
          sudo apt-get install -y dpkg-dev gnupg

      - name: Build synthetic .debs
        run: |
          set -euo pipefail
          for architecture in amd64 arm64; do
            pkg_root="dist/apt-ci/pkg-${architecture}"
            mkdir -p "${pkg_root}/DEBIAN" "${pkg_root}/usr/bin"

            printf '%s\n' '#!/usr/bin/env bash' "echo \"WorkTree CI ${architecture}\"" > "${pkg_root}/usr/bin/worktree"
            chmod +x "${pkg_root}/usr/bin/worktree"

            {
              echo "Package: worktree"
              echo "Version: 0.0.0~ci1"
              echo "Architecture: ${architecture}"
              echo "Maintainer: WorkTree CI <ci@autoexplore.ai>"
              echo "Description: Synthetic WorkTree package for deployment CI."
            } > "${pkg_root}/DEBIAN/control"

            dpkg-deb --build "${pkg_root}" "dist/apt-ci/worktree_0.0.0~ci1_${architecture}.deb"
          done

      - name: Generate temporary signing key
        env:
          GNUPGHOME: ${{ github.workspace }}/dist/apt-ci/gnupg
        run: |
          set -euo pipefail
          mkdir -p "$GNUPGHOME"
          chmod 700 "$GNUPGHOME"

          {
            echo "Key-Type: RSA"
            echo "Key-Length: 3072"
            echo "Name-Real: WorkTree CI"
            echo "Name-Email: ci@autoexplore.ai"
            echo "Expire-Date: 0"
            echo "%no-protection"
            echo "%commit"
          } > dist/apt-ci/ci-gpg.batch

          gpg --batch --generate-key dist/apt-ci/ci-gpg.batch
          key_id="$(gpg --batch --list-secret-keys --with-colons | awk -F: '$1 == "sec" { print $5; exit }')"
          echo "APT_TEST_GPG_KEY_ID=$key_id" >> "$GITHUB_ENV"

      - name: Build signed APT repo
        env:
          GNUPGHOME: ${{ github.workspace }}/dist/apt-ci/gnupg
          APT_GPG_PASSPHRASE: ${{ secrets.APT_GPG_PASSPHRASE }}
        run: |
          set -euo pipefail
          scripts/build-apt-repo.sh \
            --repo-dir dist/apt-ci/repo \
            --deb dist/apt-ci/worktree_0.0.0~ci1_amd64.deb \
            --deb dist/apt-ci/worktree_0.0.0~ci1_arm64.deb \
            --distribution stable \
            --component main \
            --architecture amd64 \
            --architecture arm64 \
            --origin WorkTree \
            --label WorkTree \
            --description "WorkTree deployment CI repository" \
            --repo-url https://example.invalid/worktree/apt \
            --gpg-passphrase "${APT_GPG_PASSPHRASE}" \
            --signing-key "${APT_TEST_GPG_KEY_ID}"

      - name: Validate generated repo
        env:
          GNUPGHOME: ${{ github.workspace }}/dist/apt-ci/verify-gnupg
        run: |
          set -euo pipefail
          test -f dist/apt-ci/repo/dists/stable/main/binary-amd64/Packages
          test -f dist/apt-ci/repo/dists/stable/main/binary-amd64/Packages.gz
          test -f dist/apt-ci/repo/dists/stable/main/binary-arm64/Packages
          test -f dist/apt-ci/repo/dists/stable/main/binary-arm64/Packages.gz
          test -f dist/apt-ci/repo/dists/stable/InRelease
          test -f dist/apt-ci/repo/dists/stable/Release.gpg
          test -f dist/apt-ci/repo/worktree-archive-keyring.gpg
          test -f dist/apt-ci/repo/worktree.sources

          grep -q "^Package: worktree$" dist/apt-ci/repo/dists/stable/main/binary-amd64/Packages
          grep -q "^Filename: pool/main/[a-z0-9]/worktree/worktree_0.0.0~ci1_amd64.deb$" dist/apt-ci/repo/dists/stable/main/binary-amd64/Packages
          grep -q "^Package: worktree$" dist/apt-ci/repo/dists/stable/main/binary-arm64/Packages
          grep -q "^Filename: pool/main/[a-z0-9]/worktree/worktree_0.0.0~ci1_arm64.deb$" dist/apt-ci/repo/dists/stable/main/binary-arm64/Packages
          grep -q "^Architectures: amd64 arm64$" dist/apt-ci/repo/dists/stable/Release
          grep -q "^URIs: https://example.invalid/worktree/apt$" dist/apt-ci/repo/worktree.sources
          grep -q "^Architectures: amd64 arm64$" dist/apt-ci/repo/worktree.sources
          grep -q '^deb \[arch=amd64,arm64 signed-by=/usr/share/keyrings/worktree-archive-keyring.gpg\] https://example.invalid/worktree/apt stable main$' dist/apt-ci/repo/worktree.list

          mkdir -p "$GNUPGHOME"
          chmod 700 "$GNUPGHOME"
          gpg --batch --no-default-keyring --keyring dist/apt-ci/repo/worktree-archive-keyring.gpg --verify dist/apt-ci/repo/dists/stable/InRelease >/dev/null

      - name: Verify generated repo with apt-secure
        run: |
          set -euo pipefail

          repo_root="$(pwd)/dist/apt-ci/repo"
          source_dir="${RUNNER_TEMP}/worktree-apt-sources.d"
          lists_dir="${RUNNER_TEMP}/worktree-apt-lists"

          mkdir -p "$source_dir"
          sudo mkdir -p "${lists_dir}/partial"

          cat > "${source_dir}/worktree-ci.sources" <<EOF
          Types: deb
          URIs: file://${repo_root}
          Suites: stable
          Components: main
          Architectures: amd64
          Signed-By: ${repo_root}/worktree-archive-keyring.gpg
          EOF

          sudo apt-get update \
            -o Dir::Etc::sourcelist=/dev/null \
            -o Dir::Etc::sourceparts="${source_dir}" \
            -o Dir::State::lists="${lists_dir}" \
            -o APT::Get::List-Cleanup=0

          apt-cache policy worktree \
            -o Dir::Etc::sourcelist=/dev/null \
            -o Dir::Etc::sourceparts="${source_dir}" \
            -o Dir::State::lists="${lists_dir}" \
            | grep -q '0.0.0~ci1'

  macos-packaging-smoke:
    name: macOS packaging smoke (${{ matrix.target_platform }})
    runs-on: ${{ matrix.runs_on }}
    timeout-minutes: 90
    strategy:
      fail-fast: false
      matrix:
        include:
          - target_platform: aarch64-darwin
            arch: arm64
            runs_on: macos-latest
          - target_platform: x86_64-darwin
            arch: x86_64
            runs_on: macos-15-intel
    steps:
      - uses: actions/checkout@v7

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Cache Rust artifacts
        uses: Swatinem/rust-cache@v2

      - name: Build release binary
        run: |
          set -euo pipefail
          cargo_config_output="$(scripts/macos-cargo-config.sh "${{ matrix.arch }}" release)"
          set --
          if [[ -n "$cargo_config_output" ]]; then
            while IFS= read -r arg; do
              set -- "$@" "$arg"
            done <<<"$cargo_config_output"
          fi
          set -- "$@" -p worktree --release --locked --features ui-gpui,gix --bins
          cargo build "$@"

      - name: Package macOS artifacts
        run: |
          set -euo pipefail
          scripts/package-macos.sh \
            --version 0.0.0-ci \
            --arch "${{ matrix.arch }}" \
            --release \
            --no-build \
            --out-dir dist

      - name: Verify tarball contents
        run: |
          set -euo pipefail
          tarball="dist/worktree-v0.0.0-ci-macos-${{ matrix.arch }}.tar.gz"
          test -f "$tarball"
          tar -tzf "$tarball" | grep -q "WorkTree.app/Contents/MacOS/worktree$"

      - name: Verify DMG launches binary
        run: |
          set -euo pipefail
          dmg="dist/worktree-v0.0.0-ci-macos-${{ matrix.arch }}.dmg"
          test -f "$dmg"

          mount_point="$(hdiutil attach "$dmg" | awk '/\/Volumes\// {print $3; exit}')"
          if [ -z "$mount_point" ]; then
            echo "Failed to determine DMG mount point." >&2
            exit 1
          fi

          "${mount_point}/WorkTree.app/Contents/MacOS/worktree" --help >/dev/null
          hdiutil detach "$mount_point"
